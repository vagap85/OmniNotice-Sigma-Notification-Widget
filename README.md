# 🔔 OmniNotice — Sigma Notification Widget

Виджет уведомлений для React. Показывает всплывающие уведомления с иконками, кнопками и плавными анимациями.



### 📦 Установка

npm install
🚀 Запуск
bash
# Запустить проект в браузере
npm run dev

# Собрать проект
npm run build

# Посмотреть собранную версию
npm run preview
🧪 Тесты
bash
# Запустить все тесты
npm run test

# Запустить тесты с отчётом о покрытии
npm run test:coverage

# Запустить тесты в режиме наблюдения (автоматический перезапуск)
npm run test:watch
## 📖Как использовать
# 1. Добавьте виджет в приложение
tsx
import NotificationCardList from './NotificationWidget/NotificationCardList';

function App() {
  return <NotificationCardList maxVisible={3} />;
}
# 2. Управляйте уведомлениями через ref
tsx
import { useRef } from 'react';
import NotificationCardList from './NotificationWidget/NotificationCardList';

function App() {
  const listRef = useRef(null);

  const showNotification = () => {
    listRef.current?.add({
      title: 'Новое уведомление',
      description: 'У вас есть новое сообщение',
      buttonText: 'Посмотреть',
      Icon: <BellIcon />,
    });
  };

  return (
    <>
      <button onClick={showNotification}>Показать уведомление</button>
      <NotificationCardList ref={listRef} maxVisible={3} />
    </>
  );
}
# 3. Методы, доступные через ref
Метод	Описание<br>
add(notification)	Добавить уведомление<br>
remove(id)	Удалить уведомление по ID<br>
⚙️ Настройки<br>
Создайте файл notification.config.ts:

ts
export default {
  maxVisible: 3,          // Максимум видимых уведомлений
  removalAnimationMs: 300 // Длительность анимации удаления (мс)
};
🧩 Структура уведомления<br>
ts
{
  id?: string,               // Уникальный ID (генерируется автоматически)<br>
  title: string,             // Заголовок<br>
  description: string,       // Описание<br>
  buttonText: string,        // Текст на кнопке<br>
  Icon?: React.ReactNode,   // Иконка (опционально)<br>
  onClose?: () => void,     // Функция при закрытии<br>
  onClick?: () => void      // Функция при клике на кнопку<br>
}

# 📋 Какие тесты я написали и зачем
19 тестов для проверки ключевой логики виджета. Вот что они проверяют:

# 1. App.test.tsx — 4 теста
Проверяем, как виджет работает внутри приложения.

Тест	Что проверяем	Зачем<br>
1	рендерит кнопку и виджет	Кнопка "Показать уведомление" появляется на странице	Убедиться, что приложение не падает при запуске<br>
2	по клику появляется уведомление	После клика появляется карточка с текстом	Проверить, что пользователь видит уведомление<br>
3	номер уведомления увеличивается	Счётчик растёт с каждым кликом: 1, 2, 3...	Убедиться, что каждое новое уведомление уникально<br>
4	может показывать несколько уведомлений	Можно добавить 3 уведомления и все видны	Проверить, что виджет поддерживает несколько уведомлений одновременно<br>
# 2. NotificationCardList.test.tsx — 7 тестов
Проверяем, как работает список уведомлений.

Тест	Что проверяем	Зачем<br>
1	рендерится без ошибок	Компонент не падает при пустом списке	Убедиться, что виджет не ломается без данных<br>
2	добавляет уведомление через ref	Метод add() создаёт новое уведомление	Проверить, что основная функция работает<br>
3	добавляет все уведомления	Можно добавить 5 уведомлений подряд	Убедиться, что нет ограничений на добавление<br>
4	удаляет уведомление по клику	Кнопка "Закрыть" удаляет карточку	Проверить, что пользователь может закрыть уведомление<br>
5	показывает несколько уведомлений	После добавления 2-х уведомлений видны оба	Убедиться, что все уведомления отображаются<br>
6	каждое уведомление имеет уникальный ID	ID уведомлений не повторяются	Проверить, что React правильно обновляет список<br>
7 Автоудаление виджета
# 3. NotificationCard.test.tsx — 7 тестов
Проверяем, как выглядит и работает отдельное уведомление.

Тест	Что проверяем	Зачем
1	рендерит заголовок и описание	На странице есть текст из пропсов	Убедиться, что данные отображаются правильно<br>
2	рендерит иконку	Иконка появляется, если её передали	Проверить, что кастомные иконки работают<br>
3	рендерится без иконки	Компонент не падает, если иконки нет	Убедиться, что иконка опциональна<br>
4	вызывает onClose при клике на крестик	При клике на "✕" вызывается функция закрытия	Проверить, что уведомление можно закрыть<br>
5	не вызывает onClose, если его нет	Компонент не падает, если onClose не передан	Убедиться, что функция опциональна<br>
6	имеет правильные ARIA-атрибуты	Есть role="alert", aria-labelledby	Проверить, что виджет доступен для скринридеров<br>
7	кнопка закрытия имеет aria-label	У кнопки есть подпись для незрячих пользователей	Убедиться, что доступность соблюдена<br>

# 4. simple.test.tsx — 1 тест
Проверяем, что тестовое окружение работает.

Тест	Что проверяем	Зачем
1	простая проверка	1 + 1 = 2	Убедиться, что Jest настроен правильно<br>
📊 Итого
Файл	Тестов	Что покрываем<br>
App.test.tsx	4	Взаимодействие пользователя с виджетом<br>
NotificationCardList.test.tsx	6	Управление списком уведомлений<br>
NotificationCard.test.tsx	8	Отображение и поведение одного уведомления<br>
simple.test.tsx	1	Проверка тестового окружения<br>
Всего	19	✅ Всё работает<br>
🐛 Что нашли благодаря тестам

# В процессе тестирования нашёл и исправил ошибки:
В процессе тестирования мы не только нашли, но и подтвердили исправление ряда моментов:

1. **Неправильные SVG-атрибуты** — React выдавал предупреждение. Разработчик заменил `stroke-linecap` на `strokeLinecap`. ✅ Исправлено.
2. **Автоматическое скрытие** — новая функция, добавленная разработчиком. Мы написали тест и убедились, что уведомления корректно исчезают через заданное время. ✅ Проверено.
3. **Компонент падал без `data-testid`** — тест не мог найти элемент. Мы переписали тест, чтобы он не зависел от тестовых атрибутов в продакшн-коде. ✅ Исправлено.
4. **`maxVisible`** — благодаря тестам подтвердили, что логика работает корректно и удаляет лишние уведомления асинхронно. ✅ Проверено.
5. **Метод `clear()`** — отсутствует. 🟡 Добавлен в план доработок.

## 🍒Заглушка «Вишенки» (задача 2.6)

### Что делает заглушка

1. **Меню каналов доставки**
   - 📧 По e-mail (активен)
   - 📱 По смс (скоро)
   - ✈️ В телеграм (скоро)
   - 💬 В MAX (скоро)

2. **Форма рассылки**
   - Тема письма (до 70 символов)
   - Сообщение
   - Получатели (через пробел)

3. **Подтверждение перед отправкой**
   - Модальное окно с информацией о канале и количестве получателей

4. **Прогресс отправки**
   - Прогресс-бар с процентами
   - Имитация задержки через `mockSendApi`

5. **Результат**
   - ✅ Успех: «Успешно отправлено N получателям»
   - ❌ Ошибка (задел на будущее)

---

### Структура папок
src/pages/Vishenka/
├── Vishenka.tsx ← главная страница
├── Vishenka.css
├── types.ts
├── components/
│ ├── ConfirmDialog.tsx ← подтверждение
│ ├── ProgressBar.tsx ← прогресс
│ └── ResultMessage.tsx ← результат
├── hooks/
│ └── useSendNotification.ts ← логика отправки
└── mocks/
├── mockChannels.ts ← список каналов
└── mockSendApi.ts ← мок-API отправки

text

---

### 🍒Заглушка «Вишенка» (задача 2.6)

Страница эмуляции сервиса «Вишенка» для тестирования сценария отправки.

Все тесты — в файле `src/__tests__/Vishenka.test.tsx`.

| # | Тест | Что проверяет | Зачем |
|---|------|---------------|-------|
| 1 | `рендерит страницу «Вишенка»` | Компонент монтируется без ошибок, есть заголовок | Защита от поломки при рефакторинге |
| 2 | `показывает 4 канала: e-mail, смс, telegram, MAX` | Все каналы из макета отображаются | Если кто-то удалит канал из `mockChannels` — тест упадёт |
| 3 | `кнопка "Отправить" отключена без данных` | Пока нет темы и получателей — кнопка disabled | Защита от отправки пустой формы |
| 4 | `кнопка активна после заполнения темы и получателей` | Как только заполнены обязательные поля — кнопка активна | Проверка логики `canSend` |
| 5 | `показывает количество добавленных адресов` | Счётчик правильно считает адреса через пробел | Проверка, что `a@mail b@mail c@mail` = 3 |
| 6 | `открывает подтверждение по клику на "Отправить"` | Появляется `ConfirmDialog` | Часть задачи 2.1 — подтверждение |
| 7 | `показывает прогресс после подтверждения` | Появляется `ProgressBar` с `role="progressbar"` | Часть задачи 2.1 — прогресс |
| 8 | `показывает успех после завершения отправки` | Полный E2E: форма → подтверждение → прогресс → успех | Главный happy-path сценарий |
| 9 | `отмена подтверждения возвращает к форме` | Клик по «Отмена» закрывает диалог | Пользователь может вернуться и редактировать |

---

### Что покрыто тестами

✅ Рендеринг страницы  
✅ Меню каналов  
✅ Валидация (disabled/enabled кнопки)  
✅ Подсчёт получателей (разделитель — пробел)  
✅ Подтверждение  
✅ Прогресс  
✅ Успешная отправка (E2E)  
✅ Отмена подтверждения

### Что НЕ покрыто (в планах)

| Сценарий | Почему нет теста |<br>
|----------|------------------|
| ❌ Ошибка отправки | В `mockSendApi` нет способа смоделировать ошибку — **нужен флаг `shouldFail` от разработчика** |<br>
| 🔁 Кнопка «Повторить» при ошибке | Не реализована в UI |<br>
| ⌨️ Escape закрывает диалог | Не реализовано в `ConfirmDialog` |<br>
| 🎯 Фокус переводится в модалку | Не реализовано (a11y) |<br>
| 🚫 Недоступные каналы отключены | Тест частично есть (каналы disabled), но не проверяет `canSend` |<br>
| 📧 Валидация email | Не реализована |<br>
| 📱 Мобильная адаптация | Нет медиа-запросов в CSS |<br>

# 🔧Технологии
React 18

TypeScript

Vite

Jest + Testing Library

CSS-модули (FLIP-анимация)

# 📊 Покрытие кода

Все файлы: Stmts 88.23% | Branch 77.14% | Funcs 93.54% | Lines 87.64% |<br>


# 📊 Приоритеты для доработки
Приоритет	Задача	Сложность<br>
🔴 Высокий	Добавить clear()	Лёгкая<br>
🔴 Высокий	Улучшить обработку ошибок	Средняя<br>
🟡 Средний	Обработка клика на кнопку	Лёгкая<br>
🟢 Низкий	CI/CD на GitHub Actions	Средняя<br>

📝 Лицензия
MIT

👤 Автор
Разработчик: Davyd Ordynskiy
Тестировщик: Evgeny Agapov
